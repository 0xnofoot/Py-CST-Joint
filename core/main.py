import sys
import os
import numpy as np

from joint_simulation import multiple
from core.util.matrix.operation import fun as m_fun, show as m_show, bool as m_bool

# multiple.absorber.vertical_bar("vertical_bar")
multiple.absorber.cross("cross")
